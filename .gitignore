.env
# OpenRouter minted runtime inference key — gitignored secret, regenerable via 'markery model mint'
.openrouter-key
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv
venv/
ENV/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
.DS_Store
csv/
*.duckdb
!data/trademarks.duckdb
!data/patents.duckdb
!data/entities.duckdb
# Ignore Claude Code runtime config but track project commands
.claude/*.json
.claude/*.local.*
.claude/*.lock
input/
output/
tools/image_enhancement/weights/
# Project generated outputs (images, PDFs, gallery HTML) — track research docs only
projects/*/input/
projects/*/output/
# Auto-generated session brief — regenerated by markery historian prepare
projects/*/BRIEF.md
# Library raw text files — multi-MB, re-acquirable via markery librarian acquire
library/works/*/raw_text.txt
library/works/*/*.txt
# Extraction staging area — regenerable, accepted passages land in excerpts.md
library/works/*/candidates.md
# Embedding index — regenerable via markery librarian index --embed
library/index.duckdb
# Context cards — regenerable via markery librarian card <query>
library/cards/

# Coverage
.coverage
.coverage.*
htmlcov/

# Built site output (regenerable via `markery site build-all`; published at deploy/P6)
/site/
projects/*/site/
