# Deferred Work Register

Items explicitly deferred — not forgotten, not blocked, just not the current priority. Each entry has a reopen trigger. When the trigger condition arises, promote the item to active work.

| ID | Deferred item | Reopen trigger |
|---|---|---|
| ~~D050~~ | ~~Phase 16 Track A Wikipedia edit review — confirm all 7 edits unreverted; close P2 and P3 ROADMAP gates~~ | ✅ Done 2026-06-07 — All 5 tracked revisions (1357391696, 1357570204, 1357918452, 1358111560, 1358151236, 1358151441) confirmed live with no `mw-reverted` tag via MediaWiki API. Rolodex revision ID retrieved from article history: **1357918452**. Library Bureau Stage 4b external link (edit 1/5, 2026-05-22) confirmed **not submitted** — no CosmoGSpacely edit exists in Library Bureau history on that date and the TSDR link is absent from the article; STATUS.md updated to reflect this. Phase 16 Archive P2 and P3 gates marked PASSED 2026-06-07. All records backfilled to `submissions.jsonl` in each project's `wikipedia/` directory. |
| ~~D049~~ | ~~Librarian MVO contracts (index, search, list, card) documented in `mvo.md` but have no corresponding tests in `tests/test_mvo.py`~~ | ✅ Done 2026-06-07 — `TestLibrarian` class added to `tests/test_mvo.py` with 9 tests covering the four contracts: `librarian index` (exit 0, "Indexed N work(s)" output), `librarian list` (exit 0, known slug present), `librarian search` (exit 0, header columns present), `librarian card` (exit 0, `# Library card:` header, quoted passage). Skip markers `requires_library` and `requires_library_index` gate on library/works presence and index.jsonl existence. |
| ~~D048~~ | ~~`markery match` invisible subcommands (`status`, `rescore`, `auto-disposition`, `preflight`) not listed in `--help`~~ | ✅ Done 2026-06-05 — `RawDescriptionHelpFormatter` epilog added to `match_main()` argparse listing all four subcommands. `markery match --help` now shows them. |
| D053 | `markery match` has no command to inspect per-entity candidate scores after generation | **What it is:** After `markery match <project> --all-serials`, there is no CLI path to view scores broken down by entity. `markery match status` shows only aggregate P50/P90 statistics. Verifying that a specific entity (e.g. Colt, P&W) appears in the candidate pool with expected scores currently requires reading `candidates.jsonl` directly. A `markery match inspect <project> [--entity NAME] [--min-score N] [--max-score N]` command would close this gap: list candidates grouped by entity, showing patent_no, trademark_serial, score, and which signal fields fired. **Why deferred:** Phase 20 P1 — score verification after `prior_brand_serials` rollout required multiple inline Python scripts against `candidates.jsonl`. No CLI command exists for per-entity score inspection. **Reopen when:** a second debugging or audit session requires per-entity candidate score inspection without direct file reads, or when Phase 20 P5 data quality audit identifies entity-level score anomalies that need a repeatable CLI path. |
| ~~D047~~ | ~~`trademarks.duckdb` has no `assignment` table — ownership-transfer chain not importable~~ | ✅ Done 2026-06-08 — `_ASSIGNMENT_DDL` added to `trademark/build.py` with explicit schema and `-- contract:` DDL comments on `serial_no`, `assignor_name`, `assignee_name`, `assignment_date`. `load_assignment(file_path, conn)` implemented following the `load_events`/`load_foreign` pattern: drops and recreates the table, inserts from `read_csv_auto(file_path)` filtered to `case_file` serials, adds four indexes. `markery trademark load-assignment --file <path>` registered in CLI. Chain query `SELECT assignor_name, assignee_name, assignment_date FROM assignment WHERE serial_no = 71246709 ORDER BY assignment_date` verified in test with Rand Kardex → Remington Rand data. 4 tests in `tests/test_build_modules.py`. Data verification with actual USPTO assignment bulk file requires the user to supply the file. |
| ~~D046~~ | ~~No CLI path to batch-enrich project-scope trademarks before candidate generation~~ | ✅ Done 2026-06-08 — `_collect_serials_from_variants(variants_path, conn_tm)` added to `trademark/enrich.py`: reads trademark_owner/trademark_search variant names from `variants.csv`, queries `owner` table with `UPPER(own_name) IN (...)` (case-insensitive), returns distinct serial_nos. `enrich_project(..., source="from-variants")` now calls this path. `--source from-variants` added to `markery trademark enrich-project` argparse choices. 5 tests in `tests/specialist/trademark/test_enrich.py` verify: matching serials returned, case-insensitive lookup, patent_assignee source excluded, missing file returns empty, no candidates.jsonl needed. |
| ~~D045~~ | ~~Instruction cards for five secondary LIBRARIAN commands: `review`, `raw-text`, `enter`, `wants-update`, `list`~~ | ✅ Done 2026-06-07 — Five stub instruction cards written in `src/markery/specialist/librarian/persona/instructions/`: `review.md` (interactive vs `--auto-accept` paths, references D032 fix), `raw-text.md` (path lookup, absent-file guidance), `enter.md` (manual ILL/physical registration, excerpts.md format), `wants-update.md` (status lifecycle, slug matching note), `list.md` (compact and verbose modes, excerpt count interpretation). |
| ~~D044~~ | ~~`markery librarian acquire` should accept the slug suggested by `search-sources`, not only the raw IA identifier~~ | ✅ Done 2026-06-07 — Two fixes in `cli.py` `cmd_acquire()`: (1) before hitting the IA API, check if `library/works/<identifier>/metadata.json` exists — if so, print "Already acquired" and exit 0 (catches the case where user passes a friendly slug that was previously acquired under that name); (2) when IA returns no title, print a specific hint: "If this is a slug from 'markery librarian search-sources', use the raw IA identifier from the IDENTIFIER column instead." The raw IA identifier requirement is not removed (IA's API requires it), but the error is now actionable rather than opaque. 3 tests in `tests/test_librarian_p3.py`. |
| ~~D001~~ | ~~Fetch remaining G09F patents (1910–1939) for information-systems project~~ | ✅ Done 2026-05-22 — 9,279 patents added across 6 windows (1910–1939); patents.duckdb now at 40,029 total. |
| D025 | Research project: photographic equipment (Kodak, Ansco, Graflex, 1890–1940) | **What it is:** A Markery research project on early American photographic equipment manufacturers. Target entities: Eastman Kodak Company, Ansco (Ansco Company / General Aniline & Film), Graflex Inc., Blair Camera Company. Key patent-trademark correspondences: Kodak's emulsion and camera mechanism patents + KODACHROME, BROWNIE, AUTOGRAPHIC marks; Ansco's roll-film patents + SPEEDEX, READYSET marks; Graflex's focal-plane shutter patents + GRAFLEX, SPEED GRAPHIC marks. CPC classes: `G03B` (photographic apparatus), `G03C` (photosensitive materials), `G03D` (photographic processing). Secondary literature: Reese Jenkins, *Images and Enterprise: Technology and the American Photographic Industry, 1839–1925* (1975) — available on IA, directly relevant to the patent-trademark methodology. Wikipedia target articles have genuine citation gaps in the 1910–1940 period. **Why deferred:** `radio-pioneers` is the selected live-test project (Phase 16 Track B). Photographic equipment is a strong second candidate with cleaner corporate histories and richer IA secondary literature than radio, but Kodak's heavy coverage in existing scholarship reduces the novelty of the research contribution. **Reopen when:** Phase 16 Track B (`radio-pioneers`) is complete and a second live-test project is selected. |
| D026 | Research project: industrial fasteners and precision tools (Snap-on, Starrett, Brown & Sharpe, 1910–1940) | **What it is:** A Markery research project on American precision tool and fastener manufacturers. Target entities: Snap-on Tools Company, L.S. Starrett Company, Brown & Sharpe Manufacturing, Illinois Tool Works. Key patent-trademark correspondences: Snap-on's socket wrench patents + SNAP-ON mark; Starrett's micrometer patents + STARRETT marks; Brown & Sharpe's precision gauge patents + MAUSER/BROWNE & SHARPE marks. CPC classes: `B25B` (tools for tightening), `B23B` (turning, boring), `G01B` (measuring instruments). Secondary literature: thinner than radio or photography, but trade catalogs are well-preserved on IA. Wikipedia articles for these companies are mostly stubs — high opportunity for original primary-source contribution. **Why deferred:** `radio-pioneers` is the selected live-test project. Precision tools have a tighter, cleaner scope than radio, making them a better candidate if a second live-test is needed in a smaller domain. Lower secondary literature availability is the main limitation. **Reopen when:** Phase 16 Track B (`radio-pioneers`) is complete and a tightly-scoped second project is preferred over photography. |
| ~~D043~~ | ~~Per-project model configuration: `project.json` should hold a `model` key; CLI should read it and set `MARKERY_MODEL` automatically when the project argument is given~~ | ✅ Done 2026-06-07 — `model: str \| None` field added to `Project` dataclass and read by `load_project()` in `common/project.py`. `_try_inject_project_model(rest)` added to `src/markery/cli.py` main entry point: scans `rest` for any arg that matches a known project directory, reads its `project.json`, and if `model` is set and `MARKERY_MODEL` is not already in `os.environ`, sets it before dispatch. `animal-marks-1930/project.json` updated with `"model": "claude-haiku-4-5-20251001"`. 4 tests in `tests/test_project_model.py`. |
| D042 | `markery match` should accept a `--serials` CLI flag to scope candidate generation without requiring project.json configuration | **What it is:** The `--serials <serial> [<serial>...]` CLI flag originally proposed for `markery match <project>`. Phase 17 P1 addressed the underlying problem via `focus_serials` in `project.json`: when set, `markery match` automatically filters to those serials (with `--all-serials` to override). This covers the primary use case. The unimplemented part is an ad-hoc CLI override for one-off generation scoped to specific serials without editing project.json. **Why still open:** The project-config approach (Phase 17 P1) is cleaner for persistent project focus. The ad-hoc `--serials` flag would be useful for exploratory generation outside a project context but has no current user. **Reopen when:** a workflow requires generating candidates for a subset of serials without modifying project.json (e.g. exploratory single-entity investigation), or when `focus_serials` project config proves inflexible for a third project's needs. |
| ~~D041~~ | ~~Figurative marks with `NULL trademark` field produce crashes or corrupt output across multiple historian and publisher code paths~~ | ✅ Done 2026-06-07 — All code paths now None-safe. Final fix (Phase 19 P2): `historian/review.py` TUI `display()` now uses `cand.get("trademark") or "(figurative)"` (line 160); queue-building filter uses `tm_text = c.get("trademark") or ""` before `.upper()` call (line 285). `markery matchmaker confirm` also handles `trademark=None` via `cand.get("trademark")`. 7 tests in `tests/specialist/matchmaker/test_confirm.py` cover TUI figurative path. |
| ~~D040~~ | ~~`markery patent signals` is misplaced in Phase 16.1 P3 spec — signals requires `candidates.jsonl` (populated in P4) and always returns "0 candidates enriched" when called before candidate generation~~ | ✅ Done 2026-06-07 — Ordering constraint added to `src/markery/specialist/historian/persona/instructions/patent-signals.md` § "When to use": "`markery patent signals <project>` reads from `candidates.jsonl` and enriches no records when called before `markery match`. Call it as step 1 of P4, immediately after `markery match` generates candidates. Calling it during P3 or earlier produces '0 candidates enriched' — technically correct but a no-op." |
| ~~D039~~ | ~~`markery matchmaker suggest-variants` should display representative patent titles alongside each candidate assignee string~~ | ✅ Done 2026-06-08 — `_get_example_titles(conn_pat, assignee_name, limit=2)` added to `matchmaker/cli.py`: queries `SELECT title, YEAR(grant_dt) FROM patents WHERE UPPER(assignee_name) = UPPER(?) LIMIT ?`. `cmd_suggest_variants` now caches titles for all top-ranked patent assignees before closing the connection, then prints `N× ASSIGNEE_NAME  (e.g. "Patent Title" (year))`. Trademark owner section unchanged (no patent-title analogue). 3 tests in `tests/test_phase11_commands.py` covering title+year return, case-insensitive lookup, and empty result for unknown assignee. |
| ~~D038~~ | ~~`markery trademark enrich` stores raw TSDR JSON but leaves structured columns NULL~~ | ✅ Done 2026-06-05 — `_parse_case_status()` updated for actual TSDR format (`trademarks[0].status.markElement`, `gsList[0].description`, `parties.ownerGroups[max_key][0].name`); `backfill_structured_fields(conn)` added to re-parse stored raw_json without re-fetching; `markery trademark reparse` CLI command exposes it. Ran against trademarks.duckdb: 154 rows backfilled, 163/167 mark_text, 167 status_cd, 165 goods_desc, 148 owner_name populated. |
| ~~D037~~ | ~~`markery matchmaker clear --data-dir <project>` — remove a project's entities and variants from `entities.duckdb`~~ | ✅ Done 2026-06-08 — `clear(data_dir, db_path, dry_run)` added to `matchmaker/entities.py`: reads entity IDs from `entities.csv`, counts matching rows in `entity_name_variant` and `company_entity`, deletes variants then entities (FK order), returns `{"entities": n, "variants": n}`. `markery matchmaker clear <project> [--dry-run] [--yes]` registered in CLI. Without `--yes`: interactive prompt. Without rows: no-op with message. 4 tests in `tests/specialist/matchmaker/test_entities.py` covering dry-run, delete, no-entities no-op, missing CSV error. |
| ~~D036~~ | ~~`markery trademark mark-status <project> [--dead-only]` — report live/dead and public-domain status for all marks in a project's scope~~ | ✅ Done 2026-06-08 — `mark_status_report(conn, tm_variants, dead_only, pd_only, pd_threshold_year)` added to `trademark/queries.py`: queries `owner JOIN case_file` for the given variant names, derives `live_dead` (cfh_status_cd ≥ 700 → dead, else live) and `public_domain` (filing year ≤ threshold), applies filters. `markery trademark mark-status <project> [--dead-only] [--pd-only]` registered in CLI; reads `variants.csv` for trademark_owner/trademark_search variant names, calls `mark_status_report`. 7 tests in `tests/specialist/trademark/test_queries.py` covering live/dead classification, public_domain flag, dead-only and pd-only filters, empty variants, and figurative mark label. |
| ~~D035~~ | ~~`markery matchmaker build` should validate CSV parse integrity for comma-containing variant names~~ | ✅ Done 2026-06-05 — source-field validation loop added to `entities.py` `build()` before insert; checks each row against `{"patent_assignee", "trademark_owner", "trademark_search"}`; prints error with row number and exits 1 on mismatch. |
| D027 | `markery project onboard <project>` — integrated new-project workflow command | **What it is:** A single command that runs the full new-project setup sequence: (1) validate entity IDs are globally unique; (2) run `suggest-variants` for each entity and print top matches; (3) run `validate-variants` against actual DB strings; (4) report in-scope trademark and patent counts; (5) run `patent coverage-check` for configured CPC classes and warn if expected counts are low. Each step either passes or prints a specific actionable error. A lighter model following this command's output could complete the P5-equivalent workflow without diagnostic SQL. **Why deferred:** `validate-variants` and `coverage-check` are being implemented now as immediate hardening. The onboarding wrapper belongs after those are stable. The P5 experience with radio-pioneers is the design input. **Trigger condition met:** `animal-marks-1930` was the third research project and required the same diagnostic cycles (D027 recurred: project init crashed, manual scaffold required; entity ID uniqueness unvalidated; variant coverage unverified until out-of-CLI DB checks). **Reopen when:** fourth research project setup, or when Phase 18+ scope allows the onboarding wrapper to be built on top of the now-stable `validate-variants` and `coverage-check` commands. |
| ~~D034~~ | ~~`markery trademark design-search <code-prefix> [--filing-before YEAR] [--goods-contains TEXT]` — discover marks by USPTO visual design code~~ | ✅ Done 2026-06-08 — `search_by_design_code(conn, code_prefix, filing_before, goods_contains, limit)` added to `trademark/queries.py`. Uses CTEs (`first_owner`, `first_goods`) to deduplicate multi-owner and multi-statement rows before the JOIN. Trailing dot stripped from prefix so `03.` and `03` both work. `markery trademark design-search <CODE_PREFIX> [--filing-before YEAR] [--goods-contains TEXT] [--limit N]` registered in CLI. Returns serial_no, mark_text (or "(figurative)"), own_name, filing_dt, goods_desc (first 100 chars) ordered by filing_dt. 6 tests in `tests/test_build_modules.py` covering column structure, word mark text, figurative label, filing-before filter, goods-contains filter, and trailing-dot prefix stripping. |
| ~~D032~~ | ~~`markery librarian review --auto-accept <slug>` — accept all candidates from an existing `candidates.md` without re-running extract~~ | ✅ Done 2026-06-07 — `--auto-accept` flag added to `markery librarian review` subparser in `cli.py`; `review()` in `extract.py` now accepts `auto_accept: bool = False`. When True: loops over pending blocks, calls `_append_to_excerpts()` for each, replaces `<!-- status: pending -->` with `<!-- status: accepted -->` in the raw text, writes back — no `input()` calls. 5 tests in `tests/test_librarian_p3.py` covering: all pending accepted, status markers updated, pre-accepted and rejected blocks not re-processed, `input()` not called, no-pending case exits cleanly. |
| ~~D033~~ | ~~`markery librarian index` should warn when a work's `excerpts.md` is non-empty but yields 0 parseable passages~~ | ✅ Done 2026-06-05 — warning added in `index_works()` loop after `_parse_excerpts()`: if result is empty and file size > 0, prints `WARNING: <slug>/excerpts.md has content but no parseable ### passages` to stderr. |
| ~~D029~~ | ~~`markery matchmaker confirm <project> <slug> [--note TEXT]` — non-interactive pair confirmation CLI~~ | ✅ Done 2026-06-07 — `markery matchmaker confirm <project> <slug> [--note TEXT]` implemented in `matchmaker/cli.py`. Parses slug to (tm_slug, patent_no), looks up candidate in `candidates.jsonl`, rejects unknown slugs (exit 1), deduplicates (exit 0 if already confirmed), writes to `confirmed.jsonl` via `write_confirmed`. Figurative marks handled via `cand.get("trademark")`. 5 tests in `tests/specialist/matchmaker/test_confirm.py`. |
| ~~D030~~ | ~~`markery historian simulate <project> [--model MODEL] [--top N]` — model simulation CLI command~~ | ✅ Done 2026-06-07 — Superseded by Phase 18 P5. `historian card --infer` performs per-candidate inference with a RECOMMENDATION/SCORE/REASONING structured output; `historian digest --infer` summarises the full project queue with inference; `historian draft` calls the model with the scaffold as context and writes a `<slug>-draft.md`. These three commands together replace the simulation script — they are more granular (per-candidate rather than bulk), CLI-first, tested, and token-logged. The inline-script bypass that prompted D030 is no longer the only path. No new code needed for closure. |
| ~~D031~~ | ~~`class_score` in `score.py` hardcodes information-systems CPC classes — misleads multi-project scoring~~ | ✅ Done 2026-06-05 — `class_score()` and `total_score()` accept optional `product_classes`/`class_hints` set; `generate_candidates()` and `rescore_candidates()` pass it through; CLI reads `class_hints` from `project.json`; `Project` dataclass and `load_project()` expose it. `animal-marks-1930/project.json` adds F02B/B60C/etc.; F02B (0.7346) now outscores G09F (0.4964). Default `PRODUCT_CLASSES` preserved for information-systems. |
| D028 | `markery trademark search-tsdr <mark-text>` — search TSDR by mark text to surface serial numbers | **What it is:** A command that takes a mark name (e.g. "ATWATERKENT", "AUDION") and searches TSDR for matching records, returning serial numbers, owner names, and filing dates. Needed when a mark is known by name but its serial number is not in `trademarks.duckdb`. **Why deferred:** TSDR's primary endpoint serves case lookups by serial number, not text search. A text-search path requires the USPTO TESS API (`developer.uspto.gov/trademark-api`) or scraping — neither is trivially integrated. The manual workaround (external TSDR search + `markery trademark fetch <serial>`) is acceptable until this recurs. **Reopen when:** two or more project setups require manual serial lookup for marks not in `trademarks.duckdb`. |
| D007 | Implement `markery patent bulk-import` (PatentsView route) | **What it is:** An alternative patent acquisition path using USPTO's PatentsView bulk data (`.tsv.gz` files downloadable from patentsview.org) instead of the EPO OPS API. PatentsView covers all granted US patents since 1976 with rich metadata — assignee names, CPC classes, abstracts — in a single download. No API key, no rate limits, fully offline once downloaded. **Why deferred:** EPO OPS incremental fetch (current approach) has been sufficient. The EPO daily quota (~10k records) is restrictive but the build runs over multiple days without issue. PatentsView only covers 1976+, so it does not help with the pre-1940 scope of current projects. The bulk `.tsv.gz` files are large (~several GB uncompressed) and would need a separate download step outside the repo. **Design:** Complete in `src/markery/specialist/patent/BULK_CSV.md`. Command: `markery patent bulk-import --tsv-dir <path> --year-start <YEAR> --year-end <YEAR> --classes <CPC> [...]`. Implementation: DuckDB `read_csv()` with `delim='\t'`, predicate pushdown for year/class filter, `patent_no` constructed as `US{number}{kind}`, insert-if-not-exists (idempotent with current schema). **Reopen when:** (1) a project needs CPC classes outside the EPO OPS coverage window, or (2) EPO OPS quota becomes a genuine blocker for a time-sensitive sweep, or (3) a project with 1976+ scope is opened where PatentsView's coverage is sufficient. |
| ~~D008~~ | ~~Historian persona stale content (Phase 7)~~ | ✅ Done — fixed in P1: `mark_case_status` → `extended_marks`, hardcoded row counts removed, path corrected to `src/markery/specialist/matchmaker/`. |
| ~~D009~~ | ~~Flesh out four new specialist personas~~ | ✅ Done — patent (P2), trademark (P3), matchmaker (P4), publisher (P5) all complete. |
| ~~D011~~ | ~~Re-enable GitHub Pages deployment~~ | ✅ Promoted to Phase 13 P2 (2026-05-22). `pages.yml` deleted 2026-05-20 after repeated deploy failures. Steps: diagnose original failure, recreate `.github/workflows/pages.yml`, verify site builds cleanly with `markery site build`, re-enable Pages in repository settings. |
| ~~D022~~ | ~~Add "Built with Markery" footer to publisher-generated HTML pages~~ | ✅ Promoted to Phase 13 P2 (2026-05-22). Each page built by `markery site build` should include a small footer crediting the tool with a link to the repository. Implementation: add a footer block to the Jinja/render template in `publisher/render.py`; footer text and repo URL should be configurable (or default to the GitHub repo). Natural to do alongside D011. |
| ~~D010~~ | ~~Add Smead Mfg. and WHEELDEX owner entities to information-systems project~~ | ✅ Done 2026-05-22 — entity_id 6: Smead Manufacturing Company (serial 71403472); entity_id 7: Scholfield Service, Inc. (serial 71321669, WHEELDEX). Both added to entities.csv and variants.csv. |
| ~~D018~~ | ~~Trademark persona instruction cards for `enrich-project`, `fetch`, `status`, and `verify-credentials`~~ | ✅ Promoted to Phase 12 P6 (2026-05-22). Four trademark subcommands lack instruction cards: `enrich-project` (batch TSDR fetch for all marks in a project's confirmed/candidates — distinct from `enrich` which fetches a single serial; the distinction is particularly likely to confuse because both commands exist and only `enrich.md` documents one of them), `fetch` (TSDR-only fetch into `extended_marks`, distinct from `enrich` which also fetches the image), `status` (row counts for all trademark tables), `verify-credentials`. |
| ~~D021~~ | ~~Incorporate model-agnosticism framework into DESIGN.md~~ | ✅ Done 2026-05-22 — "Model-Agnosticism by Design" section added to DESIGN.md: two-axis matrix, three principles, MVO definition and task classification table, Phase 11 convergence note. |
| ~~D023~~ | ~~Wikipedia Stage 4c — inline citation in Chicago Pneumatic article~~ | ✅ Done 2026-06-06 — Rev 1358151236 live, unreverted (confirmed 2026-06-07). Sentence added to History section citing USPTO Serial No. 71299042. Recorded in `projects/monthly-image-review/wikipedia/submissions.jsonl`. |
| ~~D024~~ | ~~Wikipedia Stage 4d — second article (Remington Rand or Soundex)~~ | ✅ Done 2026-06-06 — Soundex article chosen. Rev 1358151441 live, unreverted (confirmed 2026-06-07). SOUNDEX trademark citation (Serial No. 71246709, March 1927, Rand Kardex Bureau) added. Recorded in `projects/information-systems/wikipedia/submissions.jsonl`. |
| ~~D020~~ | ~~LIBRARIAN specialist — sixth specialist for repository-scoped secondary literature~~ | ✅ Promoted to Phase 15 (2026-05-25); closed 2026-06-01. Phase 15 complete: `markery librarian` with 13 commands, keyword + semantic search, historian context card, 42 unit tests passing. | **Why premature:** Only one project (`information-systems`) has a `references/` directory, and it is currently empty. The two blocking conditions must both be true before this is worth building. **Blocking condition 1:** `references/` format is proven across at least two distinct projects with curated, annotated excerpts — not just stub directories. This condition validates that the per-project `references/` convention is stable enough to define a corpus schema against. **Blocking condition 2:** The historian demonstrates a concrete retrieval need that cannot be met by reading a single project's `references/` directory directly — i.e., a session where the historian needs to draw on sources held in a *different* project, or needs to search across all known sources by keyword or concept. **What LIBRARIAN would own:** A shared `library/` directory at repo root (outside any project). Structure: `library/works/<author-title-slug>/metadata.json` (bibliographic record), `library/works/<author-title-slug>/excerpts.md` (annotated passages), `library/works/<author-title-slug>/index.md` (chapter/section map). Each project's `references/` symlinks or cites works in `library/`. **Retrieval design:** Two tiers — (1) keyword search across all `excerpts.md` files (immediate, no dependencies); (2) semantic search over vector embeddings stored in DuckDB's vector extension, Chroma, or LanceDB (requires embedding step at ingest time). Historian interface: `search_library(query) -> list[Excerpt]`. Full design in `archive/SPECIALIST_REVIEW-2026-05-19.md` §"Librarian Specialist — Future (Phase 7+)". **Reopen when** both blocking conditions are met — check at the start of any session that touches `references/` in a second project. |
| ~~D019~~ | ~~Deduplicate read functions across matchmaker modules~~ | ✅ Promoted to Phase 12 P1 (2026-05-22). P4 added `queries.py` with `read_confirmed`, `read_rejected`, `read_pipeline_state`. The same logic exists in `link.py` (`read_confirmed`, `read_rejected`) and `pipeline.py` (`read_state`). Neither imports from `queries.py`. Fix: make `link.py` and `pipeline.py` import their read functions from `queries.py` instead of defining them locally. Low risk — current duplication is functional but creates two sources of truth. |
| ~~D016~~ | ~~Remove `migrate-figures` from patent CLI~~ | ✅ Promoted to Phase 12 P1 (2026-05-22). `markery patent migrate-figures` is a one-time schema migration (disk PNGs → BLOB storage) completed in Phase 7. The command is still registered and appears in `--help`. Cleanup: confirm no projects have on-disk PNGs remaining, then remove `cmd_migrate_figures`, the argparse entry, and the dispatch table entry from `patent/cli.py`. |
| ~~D017~~ | ~~Patent persona instruction cards for `signals`, `fetch`, and `verify-credentials`~~ | ✅ Promoted to Phase 12 P6 (2026-05-22). Three patent subcommands lack instruction cards in `patent/persona/instructions/`: `signals` (text signal enrichment), `fetch` (batch figure fetch for a project), `verify-credentials`. `signals` and `fetch` are used in active research sessions; a partial description exists in the historian's request cards (`patent-signals.md`, `figure-fetch.md`). |
| ~~D014~~ | ~~Version number not surfaced~~ | ✅ Done 2026-05-22 — bumped to 0.2.1a0 (Phase 10 completion); `markery --version` / `-V` flag added to CLI. |
| ~~D015~~ | ~~DESIGN.md "Phase 7" internal jargon~~ | ✅ Done 2026-05-22 — replaced with neutral description in §Scope-Neutral Databases. |
| ~~D002~~ | ~~Referenced images — switch from base64-embedded to file references~~ | ✅ Done — `_img_src()` in `publisher/render.py`; `build_site()` writes `out/images/{marks,patents}/`; render functions accept `images_dir` |
| ~~D003~~ | ~~Patent drawings extraction from PDF figures~~ | ✅ Done — `[[figure:patent_no]]` in `_render_markdown()` resolves via `figure_index`; `<figure class="patent-figure">` rendered in essays |
| ~~D004~~ | ~~Events table from `event.csv`~~ | ✅ Done — `events` DDL in `_ENRICHMENT_DDL`; `load_events()`; `get_events()`; `markery trademark load-events` |
| ~~D005~~ | ~~Foreign application data (Madrid Protocol records)~~ | ✅ Done — `foreign_app` DDL in `_ENRICHMENT_DDL`; `load_foreign_app()`; `get_foreign_apps()`; `markery trademark load-foreign` |
| ~~D006~~ | ~~Scoring refinement for company-name marks~~ | ✅ Done — `is_company_name_mark()` in `specialist/matchmaker/score.py`; applied as hard exclusion in `generate_candidates()` |
