# Deferred Work Register

Items explicitly deferred — not forgotten, not blocked, just not the current priority. Each entry has a reopen trigger. When the trigger condition arises, promote the item to active work.

| ID | Deferred item | Reopen trigger |
|---|---|---|
| ~~D001~~ | ~~Fetch remaining G09F patents (1910–1939) for information-systems project~~ | ✅ Done 2026-05-22 — 9,279 patents added across 6 windows (1910–1939); patents.duckdb now at 40,029 total. |
| D007 | Implement `markery patent bulk-import` (PatentsView route) | Design complete in `src/markery/specialist/patent/BULK_CSV.md` (P6, 2026-05-20). Source: PatentsView `.tsv.gz` files. Command signature: `markery patent bulk-import --tsv-dir <path> --year-start <YEAR> --year-end <YEAR> --classes <CPC> [...]`. Key implementation points: DuckDB `read_csv()` with `delim='\t'`, predicate pushdown for year/class filtering, `patent_no` constructed as `US{number}{kind}`, insert-if-not-exists (idempotent). Reopen when: a project needs a patent class sweep infeasible via EPO OPS rate limits, or an offline environment is required. |
| ~~D008~~ | ~~Historian persona stale content (Phase 7)~~ | ✅ Done — fixed in P1: `mark_case_status` → `extended_marks`, hardcoded row counts removed, path corrected to `src/markery/specialist/matchmaker/`. |
| ~~D009~~ | ~~Flesh out four new specialist personas~~ | ✅ Done — patent (P2), trademark (P3), matchmaker (P4), publisher (P5) all complete. |
| D011 | Re-enable GitHub Pages deployment | `pages.yml` deleted 2026-05-20 after repeated deploy failures. Reopen when ready to publish the information-systems site publicly. Steps: diagnose original failure, recreate `.github/workflows/pages.yml`, verify site builds cleanly with `markery site build`, re-enable Pages in repository settings. |
| ~~D010~~ | ~~Add Smead Mfg. and WHEELDEX owner entities to information-systems project~~ | ✅ Done 2026-05-22 — entity_id 6: Smead Manufacturing Company (serial 71403472); entity_id 7: Scholfield Service, Inc. (serial 71321669, WHEELDEX). Both added to entities.csv and variants.csv. |
| D018 | Trademark persona instruction cards for `enrich-project`, `fetch`, `status`, and `verify-credentials` | Four trademark subcommands lack instruction cards: `enrich-project` (batch TSDR fetch for all marks in a project's confirmed/candidates — distinct from `enrich` which fetches a single serial; the distinction is particularly likely to confuse because both commands exist and only `enrich.md` documents one of them), `fetch` (TSDR-only fetch into `extended_marks`, distinct from `enrich` which also fetches the image), `status` (row counts for all trademark tables), `verify-credentials`. Reopen in the next persona completeness pass after Phase 8. |
| D021 | Incorporate model-agnosticism framework into DESIGN.md | Full framework (two-axis task classification, MVO concept, three design principles, task tier table, tool convergence table) is in `archive/DESIGN-REVIEW-2026-05-21.md`. Core ideas are captured in `CONTEXT.md` at a high level. Reopen: next substantive edit of `DESIGN.md`, or when beginning Phase 11 — apply the MVO concept to verify each tool's output is code-checkable or explicitly mark it model-sensitive. |
| D020 | LIBRARIAN specialist — sixth specialist for repository-scoped secondary literature | Reopen when: (1) `references/` format is proven across at least two projects with curated excerpts, AND (2) the historian demonstrates a need for cross-work retrieval that a single project's `references/` directory cannot satisfy. Full design in `archive/SPECIALIST_REVIEW-2026-05-19.md` §"Librarian Specialist — Future (Phase 7+)". Concept: `library/works/<author-title>/` holding `metadata.json`, `excerpts.md`, `index.md`; retrieval via keyword search (immediate) or semantic search over vector embeddings (DuckDB vector extension, Chroma, or LanceDB); historian calls `search_library(query) -> list[Excerpt]`. |
| D019 | Deduplicate read functions across matchmaker modules | P4 added `queries.py` with `read_confirmed`, `read_rejected`, `read_pipeline_state`. The same logic exists in `link.py` (`read_confirmed`, `read_rejected`) and `pipeline.py` (`read_state`). Neither imports from `queries.py`. Reopen in the next matchmaker cleanup pass: make `link.py` and `pipeline.py` import their read functions from `queries.py` instead of defining them locally. Low risk, low priority — current duplication is functional but creates two sources of truth. |
| D016 | Remove `migrate-figures` from patent CLI | `markery patent migrate-figures` is a one-time schema migration (disk PNGs → BLOB storage) completed in Phase 7. The command is still registered and appears in `--help` alongside regular operations. Reopen when ready to do a cleanup pass on the patent CLI: confirm no projects have on-disk PNGs remaining, then remove `cmd_migrate_figures`, the argparse entry, and the dispatch table entry from `patent/cli.py`. |
| D017 | Patent persona instruction cards for `signals`, `fetch`, and `verify-credentials` | Three patent subcommands lack instruction cards in `patent/persona/instructions/`: `signals` (text signal enrichment), `fetch` (batch figure fetch for a project), `verify-credentials`. `signals` and `fetch` are used in active research sessions; a partial description exists in the historian's request cards (`patent-signals.md`, `figure-fetch.md`). Reopen in the next persona completeness pass after Phase 8. |
| D014 | Version number not surfaced | `pyproject.toml` sets `version = "0.2.0a0"` and `__init__.py` exports `__version__`. Neither appears anywhere a user or agent would see it — no `markery --version` flag, no README header, no STATUS.md entry. Reopen when deciding whether to maintain semantic versioning actively. If version is not maintained, remove both entries to eliminate the noise. |
| D015 | DESIGN.md "Phase 7" internal jargon | `DESIGN.md §Scope-Neutral Databases` references "Phase 7 (database review, 2026-05-20)" — an internal session label with no definition in any root document. Low priority cosmetic issue. Reopen on the next substantive edit of that section; replace with a neutral description of when the change was made. |
| ~~D002~~ | ~~Referenced images — switch from base64-embedded to file references~~ | ✅ Done — `_img_src()` in `publisher/render.py`; `build_site()` writes `out/images/{marks,patents}/`; render functions accept `images_dir` |
| ~~D003~~ | ~~Patent drawings extraction from PDF figures~~ | ✅ Done — `[[figure:patent_no]]` in `_render_markdown()` resolves via `figure_index`; `<figure class="patent-figure">` rendered in essays |
| ~~D004~~ | ~~Events table from `event.csv`~~ | ✅ Done — `events` DDL in `_ENRICHMENT_DDL`; `load_events()`; `get_events()`; `markery trademark load-events` |
| ~~D005~~ | ~~Foreign application data (Madrid Protocol records)~~ | ✅ Done — `foreign_app` DDL in `_ENRICHMENT_DDL`; `load_foreign_app()`; `get_foreign_apps()`; `markery trademark load-foreign` |
| ~~D006~~ | ~~Scoring refinement for company-name marks~~ | ✅ Done — `is_company_name_mark()` in `specialist/matchmaker/score.py`; applied as hard exclusion in `generate_candidates()` |
