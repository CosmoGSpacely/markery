# Deferred Work Register

Items explicitly deferred — not forgotten, not blocked, just not the current priority. Each entry has a reopen trigger. When the trigger condition arises, promote the item to active work.

Resolved, killed, and promoted entries through 2026-06-14 are archived to `archive/DEFERRED-2026-06-14.md`. This register holds only the currently open items.

| ID | Deferred item | Reopen trigger |
|---|---|---|
| D070 | Phase 24 P6 — web hosting and deployment | **What it is:** a repeatable `markery`-driven deploy that publishes `site/` to a real static host at stable public URLs, with base-URL/path rewriting so internal links and the canonical/sitemap tags resolve. GitHub Pages was dropped (2026-06-21); candidates are Cloudflare Pages / Netlify; URL scheme (subpath vs subdomain) undecided. Full text in `archive/ROADMAP-2026-06-23.md` (Phase 24 P6). **Why deferred:** the platform foundations (code/library/data + the agentic loops) come first; the site builds and validates locally. **Reopen when:** ready to publish the Markery portal publicly (also unblocks the canonical/sitemap/GEO tags that activate with `--base-url`). |
| D071 | Phase 24 P7 — generative-engine optimization (GEO) | **What it is:** make built sites discoverable/citable by AI search — emit an AI-crawler-permissive `robots.txt`, `sitemap.xml`, canonical URLs (sitemap/canonical code exists, activates with `--base-url`), and valid **schema.org JSON-LD** for entity/essay/patent/trademark pages; covered by tests. Full text in `archive/ROADMAP-2026-06-23.md` (Phase 24 P7). **Why deferred:** pairs naturally with hosting (D070); not on the critical path to the platform/loops. **Reopen when:** the site is being hosted publicly (D070), or AI-search citation becomes a goal. |
| D072 | People — essays + rendering (the narrative half) | **What it is:** the *presentation* half of the old Phase 25. **Data-model half moved into Phase 28** (people as first-class data-layer entities: inventors from `patent_inventors`, founders, stable slugs, auto-registered). What remains deferred: HISTORIAN **person essays + company-formation essays**, and PUBLISHER rendering — a "People" nav/section, a company-formation essay on each Company page, and inventor/registrant links on cards (SITE-REVIEW #6/#16). Full original P1–P4 plan in `archive/ROADMAP-2026-06-23.md` (Phase 25). **Why deferred:** depends on projects/sites (being archived/rebuilt) and is additive narrative depth, not foundational. **Reopen when:** people exist in the data model (Phase 28) **and** the rebuilt platform/projects are ready to surface per-person narrative. |
| D028 | Finalize `markery trademark search-tsdr <mark-text>` against the live USPTO ODP text-search endpoint | **What it is:** A text-search path that takes a mark name (e.g. "KODACHROME", "STARRETT") and returns matching serial numbers, owner names, and filing dates — removing the manual external-TSDR-lookup bypass during project setup. **Current state (Phase 23 P3):** The command is **built and registered** under `markery trademark search-tsdr` with graceful degradation — mocked-HTTP tests pass, and it exits non-zero with an actionable manual-fallback message when the key is absent or the API is unavailable. The live endpoint path (`odp_search.py`, `_SEARCH_PATH="/api/v1/trademarks/search"`) is **provisional and unverified**. **Why deferred:** No official USPTO brand-text search API is reachable with the current TSDR key — TSDR's primary endpoint is serial-keyed; the ODP API requires an ID.me-verified myUSPTO API key (`USPTO_ODP_API_KEY`); `tmsearch.uspto.gov` is a WAF-gated SPA; third-party APIs are paid. P1 (`photographic-equipment`) and P2 (`precision-tools`) did not need it — the target marks were already in the local `trademarks.duckdb` and resolved via `matchmaker suggest-variants`. The user is escalating the ID.me↔myUSPTO linking with USPTO support (no resolution as of 2026-06-18). **Reopen when:** the USPTO ODP/ID.me API key (`USPTO_ODP_API_KEY`) is provisioned — then verify the live endpoint returns serial/owner/filing for a known mark, confirm the actionable-failure path, finalize the MVO contract + tests, and close D028. |
| D068 | Limit per-patent detail pages to a relevant subset | **What it is:** Phase 26 P3/SITE-REVIEW #11 added a detail page per record. For trademark-heavy projects this is fine, but radio-pioneers generates **~2,026 patent detail pages** (one per patent of RCA/Westinghouse/etc.), dominating the build (4,797 total pages) and bloating the artifact. **Why deferred:** the portal/build works and `site check` is clean; the user flagged 2,026 as too many but asked to hold. **Likely fix:** render patent detail pages only for a relevant subset (e.g. confirmed-pair patents + project-focus/class-scoped patents), and have non-detailed patent cards link to the gallery anchor or omit the link; keep trademark detail pages as-is (counts small). Decide the subset rule with the user. **Reopen when:** the user is ready to trim the patent detail-page set (next site/portal work session). |
| D075 | Backfill design-mark images for annual-review years 1929 & 1930 | **What it is / status:** the per-year design-mark image backfill **tool is built** — `markery trademark enrich-year <year> [--limit N]` fetches TSDR drawings for figurative marks (`mark_draw_cd '3%'`) filed in a year that lack one. **1928 is done** (266/266 fetched live, Phase 31). **Remaining:** run the same sweep for **1929** (~247 missing) and **1930** (~233 missing) — each is a long, rate-limited live run (`markery trademark enrich-year 1929` / `... 1930`). **Why deferred:** purely an acquisition sweep (operational, not code), best run deliberately. **Reopen when:** preparing 1929/1930 for publication. |
| D074 | eBay for-sale artifact leads (Browse API + Marketplace Insights) | **What it is:** the eBay source class from the Phase 30 plan — `librarian ebay search` (Browse API + OAuth, key-gated) producing **for-sale artifact lead cards** (hotlinked eBay-hosted thumbnail + title + price + "View listing on eBay →", "© seller, via eBay" credit; never copied/PD media, never `[[media:]]`), plus `wants` entries; purchase is always a human gate, Markery never transacts. Sold/completed prices would need eBay's **Marketplace Insights API** (a restricted grant requiring business justification — the only ToS-compliant automated route; WatchCount/Terapeak/Advanced-Search scraping is off the table / human-only). Full design in `HISTORIAN_REVIEW.md` §4c. **Why deferred:** eBay is market-signal leads, not library acquisition — out of scope for the discovery loop's "grow the rights-cleared library" core; it adds an OAuth app + a restricted-API application + a separate display frame. **Reopen when:** the discovery loop is running on free sources and there's appetite to surface for-sale artifact leads (and, if sold-price automation is wanted, after the Marketplace Insights grant is approved). |
| D073 | Migrate the library catalog from `catalog.jsonl` to a DuckDB table | **What it is:** Phase 29 keeps the library catalog as flat JSONL (`catalog.jsonl` item-level + `index.jsonl` passage-level), with the loop loading it into an in-memory dict (keyed by `source_id`/`sha256`) for O(1) dedup and rewriting atomically (last-row-wins per id). That is the right "don't over-engineer" choice at hundreds–low-thousands of items. **Why deferred:** flat files are simpler and adequate at current scale; a DB adds migration + query surface not yet needed. **Reopen when:** the catalog needs cross-collection relational queries (works + media + records together), or grows past ~low-tens-of-thousands of items, or the discovery/spawning loops hit write-contention / O(n)-rewrite cost on frequent ticks. Then move the catalog into a DuckDB table (the embeddings already live in `index.duckdb`), keeping the JSONL as an export. |
| D007 | Implement `markery patent bulk-import` (PatentsView route) | **What it is:** An alternative patent acquisition path using USPTO's PatentsView bulk data (`.tsv.gz` files downloadable from patentsview.org) instead of the EPO OPS API. PatentsView covers all granted US patents since 1976 with rich metadata — assignee names, CPC classes, abstracts — in a single download. No API key, no rate limits, fully offline once downloaded. **Why deferred:** EPO OPS incremental fetch (current approach) has been sufficient. The EPO daily quota (~10k records) is restrictive but the build runs over multiple days without issue. PatentsView only covers 1976+, so it does not help with the pre-1940 scope of current projects. The bulk `.tsv.gz` files are large (~several GB uncompressed) and would need a separate download step outside the repo. **Design:** Complete in `src/markery/specialist/patent/BULK_CSV.md`. Command: `markery patent bulk-import --tsv-dir <path> --year-start <YEAR> --year-end <YEAR> --classes <CPC> [...]`. Implementation: DuckDB `read_csv()` with `delim='\t'`, predicate pushdown for year/class filter, `patent_no` constructed as `US{number}{kind}`, insert-if-not-exists (idempotent with current schema). **Reopen when:** (1) a project needs CPC classes outside the EPO OPS coverage window, or (2) EPO OPS quota becomes a genuine blocker for a time-sensitive sweep, or (3) a project with 1976+ scope is opened where PatentsView's coverage is sufficient. |
| D078 | `trademark tech-marks` — free-model goods/services judgment to refine technological-mark triage | **What it is:** PUBLISHER_REVIEW stage 1 planned a `markery trademark tech-marks --year <Y>` command combining the deterministic US-class gate with a **free-model goods/services judgment** ("does this G&S describe something a utility patent would cover — apparatus/device/method — vs a consumable/material/content/simple-article?"), emitting a ranked list of technological design marks with reasons. **What we built instead:** the deterministic US-class gate (review cards' `⚙ Technology` badge) + the **richness signal** (`matchmaker richness` / `seed-pairs`: owner↔assignee patent overlap), which is an *independent, arguably stronger* triage signal (it catches class-gate false negatives like watches/pencils). So the free-model goods judgment was never built. **Why deferred:** richness + the class gate have proven sufficient for triage so far (the spawn loop uses them, not goods-text). The goods judgment would mainly help marks with *no* patent overlap, where richness is silent. **Reopen when:** the class-gate + richness triage proves insufficient — e.g. spawning needs to surface technological marks whose owners hold no (fetched) patents, where a goods-text judgment is the only signal. Then build `trademark tech-marks` per PUBLISHER_REVIEW §5's two-part rule, on the free model chain (D077-style). |
| D079 | Rich vs small-company project-type branching in the spawn loop | **What it is:** PUBLISHER_REVIEW stage 5 planned the spawn to branch on richness — **Rich** (many entities + many good matches across a CPC subclass → a *technology-area* project spanning multiple firms) vs **Sparse** (essentially one company → a *small-company* project). **What we built instead (Phase 32 P4):** the spawn produces **one `match-review-essay` per entity** (one company, its marks↔patents, essay, media, site) behind the tiered batch gate. The multi-firm "technology-area" project type and the rich/sparse branch were not built. **Why deferred:** one-project-per-entity is the simpler, proven MVP and matches the current corpus/coverage reality; a technology-area project needs the applicant-fetch coverage (PATENT_REVIEW) mature enough that a whole subclass is well-evidenced across firms. **Reopen when:** a technology-area project spanning multiple companies in one CPC subclass is wanted (and corpus coverage for that subclass is broad enough to support it) — then add the richness branch + a technology-area project type/scaffold to `spawn_graph.py`. |
