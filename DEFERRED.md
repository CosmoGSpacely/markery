# Deferred Work Register

Items explicitly deferred — not forgotten, not blocked, just not the current priority. Each entry has a reopen trigger. When the trigger condition arises, promote the item to active work.

Resolved, killed, and promoted entries through 2026-06-14 are archived to `archive/DEFERRED-2026-06-14.md`. This register holds only the currently open items.

| ID | Deferred item | Reopen trigger |
|---|---|---|
| D028 | Finalize `markery trademark search-tsdr <mark-text>` against the live USPTO ODP text-search endpoint | **What it is:** A text-search path that takes a mark name (e.g. "KODACHROME", "STARRETT") and returns matching serial numbers, owner names, and filing dates — removing the manual external-TSDR-lookup bypass during project setup. **Current state (Phase 23 P3):** The command is **built and registered** under `markery trademark search-tsdr` with graceful degradation — mocked-HTTP tests pass, and it exits non-zero with an actionable manual-fallback message when the key is absent or the API is unavailable. The live endpoint path (`odp_search.py`, `_SEARCH_PATH="/api/v1/trademarks/search"`) is **provisional and unverified**. **Why deferred:** No official USPTO brand-text search API is reachable with the current TSDR key — TSDR's primary endpoint is serial-keyed; the ODP API requires an ID.me-verified myUSPTO API key (`USPTO_ODP_API_KEY`); `tmsearch.uspto.gov` is a WAF-gated SPA; third-party APIs are paid. P1 (`photographic-equipment`) and P2 (`precision-tools`) did not need it — the target marks were already in the local `trademarks.duckdb` and resolved via `matchmaker suggest-variants`. The user is escalating the ID.me↔myUSPTO linking with USPTO support (no resolution as of 2026-06-18). **Reopen when:** the USPTO ODP/ID.me API key (`USPTO_ODP_API_KEY`) is provisioned — then verify the live endpoint returns serial/owner/filing for a known mark, confirm the actionable-failure path, finalize the MVO contract + tests, and close D028. |
| D069 | Add the remaining public-domain media sources (LoC, NARA, DPLA, Internet Archive media) | **What it is:** Phase 24 P2 delivered the media-enrichment capability with one source — **Wikimedia Commons** (`sources/commons.py` + `librarian media-*`). The survey (`archive/MEDIA-SOURCES-REVIEW-2026-06-22.md`) also specced five others: **Library of Congress** (`?fo=json`, rights field), **NARA** (catalog.archives.gov API, useRestriction), **DPLA** (api.dp.la, rightsstatements.org URIs, key required), and **Internet Archive media** (archive.org metadata `licenseurl`/`possible-copyright-status`; IA text is already integrated). **Why deferred:** Commons has the broadest coverage of period company/person/product imagery and satisfied the P2 gate; the others are additive. Each is a new adapter following the `commons.py` pattern (search → resolve to an admitted license → `media.acquire_*` → same metadata schema). **Reopen when:** a project needs imagery Commons lacks (e.g. US-gov photos/films via NARA, LC Prints & Photographs maps), or when broadening media coverage becomes a priority. Honor the same admission policy (PD/CC0/CC-BY/CC-BY-SA, provenance + license captured). |
| D068 | Limit per-patent detail pages to a relevant subset | **What it is:** Phase 26 P3/SITE-REVIEW #11 added a detail page per record. For trademark-heavy projects this is fine, but radio-pioneers generates **~2,026 patent detail pages** (one per patent of RCA/Westinghouse/etc.), dominating the build (4,797 total pages) and bloating the artifact. **Why deferred:** the portal/build works and `site check` is clean; the user flagged 2,026 as too many but asked to hold. **Likely fix:** render patent detail pages only for a relevant subset (e.g. confirmed-pair patents + project-focus/class-scoped patents), and have non-detailed patent cards link to the gallery anchor or omit the link; keep trademark detail pages as-is (counts small). Decide the subset rule with the user. **Reopen when:** the user is ready to trim the patent detail-page set (next site/portal work session). |
| D007 | Implement `markery patent bulk-import` (PatentsView route) | **What it is:** An alternative patent acquisition path using USPTO's PatentsView bulk data (`.tsv.gz` files downloadable from patentsview.org) instead of the EPO OPS API. PatentsView covers all granted US patents since 1976 with rich metadata — assignee names, CPC classes, abstracts — in a single download. No API key, no rate limits, fully offline once downloaded. **Why deferred:** EPO OPS incremental fetch (current approach) has been sufficient. The EPO daily quota (~10k records) is restrictive but the build runs over multiple days without issue. PatentsView only covers 1976+, so it does not help with the pre-1940 scope of current projects. The bulk `.tsv.gz` files are large (~several GB uncompressed) and would need a separate download step outside the repo. **Design:** Complete in `src/markery/specialist/patent/BULK_CSV.md`. Command: `markery patent bulk-import --tsv-dir <path> --year-start <YEAR> --year-end <YEAR> --classes <CPC> [...]`. Implementation: DuckDB `read_csv()` with `delim='\t'`, predicate pushdown for year/class filter, `patent_no` constructed as `US{number}{kind}`, insert-if-not-exists (idempotent with current schema). **Reopen when:** (1) a project needs CPC classes outside the EPO OPS coverage window, or (2) EPO OPS quota becomes a genuine blocker for a time-sensitive sweep, or (3) a project with 1976+ scope is opened where PatentsView's coverage is sufficient. |
